#include <bits/stdc++.h>

using namespace std;
const int N = 0;

class Solution {
public:
    void nextPermutation(vector<int> &num) {
        next_permutation(num.begin(), num.end());
    }
};

int main() {

    return 0;
}

